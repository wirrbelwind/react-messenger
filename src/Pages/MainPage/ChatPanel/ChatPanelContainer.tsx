import React from 'react';
import ChatPanelView from './ChatPanelView';

const ChatPanelContainer = () => {

	return (
		<ChatPanelView
			items={
				[
					{ imgURL: 'asdjkhasdka', onClick: () => {} }
				]
			} />
	);
};

export default ChatPanelContainer;